{
  "name": "agaplan-monorepo",
  "version": "1.0.0",
  "description": "AgaPlan - Monorepo con Backend, Frontend Web y Mobile",
  "private": true,
  "workspaces": [
    "backend",
    "frontend-web",
    "mobile",
    "shared"
  ],
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:web": "cd frontend-web && npm run dev",
    "dev:mobile": "cd mobile && npm run start",
    "build:backend": "cd backend && npm run build",
    "build:web": "cd frontend-web && npm run build",
    "build:mobile": "cd mobile && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend-web && npm install && cd ../mobile && npm install && cd ../shared && npm install",
    "clean": "rm -rf node_modules backend/node_modules frontend-web/node_modules mobile/node_modules shared/node_modules",
    "test": "echo \"Tests will be implemented in each workspace\"",
    "lint": "echo \"Linting will be implemented in each workspace\""
  },
  "keywords": [
    "monorepo",
    "nodejs",
    "react",
    "react-native",
    "expo",
    "typescript",
    "tailwindcss"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
